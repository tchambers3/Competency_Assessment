<% if @inactive_questions.empty? %>
  <p><em>There are no inactive questions at this time.</em></p>
<% else %>
    <h3>Inactive questions</h3>
    <table border="1">
      <thead>
        <tr>
          <th>Name</th>
          <th>&nbsp;</th>
          <th>&nbsp;</th>
        </tr>
      </thead>
      <tbody>
        <% @inactive_questions.each do |q| %>
          <tr>
            <td><%= link_to q.name, question_path(q) %></td>
            <td><%= link_to "Edit", edit_question_path(q) %></td>
            <td>
              <%= link_to "Delete", question_path(q),
                  method: 'delete',
                  data: {confirm: "Are you sure you want to permanently delete #{q.question}?"} %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
<% end %>
