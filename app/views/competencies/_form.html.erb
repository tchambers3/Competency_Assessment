<%= form_for(@competency) do |f| %>
  <% if @competency.errors.any? %>
    <div>
      <strong><%= pluralize(@competency.errors.count, "error") %> prohibited this book from being saved:</strong>
      <ul>
        <% @competency.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :active %><br>
    <%= f.check_box :active %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
